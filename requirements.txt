# VA Loan Concierge — Python dependencies (requires Python 3.12+)

# ── Azure AI Foundry (unified SDK: project client, agents, evaluations) ──────
azure-ai-projects>=2.0.1

# ── Authentication ────────────────────────────────────────────────────────────
azure-identity>=1.25.0
aiohttp>=3.9.0          # async HTTP transport for azure-identity (DefaultAzureCredential)

# ── Backend server ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.29.0

# ── Azure AI Search (Knowledge Base creation via Foundry IQ SDK) ──────────────
azure-search-documents==11.7.0b2

# ── HTTP (ARM management API calls for connection provisioning) ───────────────
requests>=2.32.0

# ── Observability (OpenTelemetry + Azure Monitor) ────────────────────────────
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-requests>=0.46b0
opentelemetry-instrumentation-aiohttp-client>=0.46b0
azure-monitor-opentelemetry-exporter>=1.0.0b27

# ── Environment management ────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
